#!/bin/sh
s() {
  n=$(($1)); i=${i:-1}; while [ "$n" -le $(($2-1)) ]; do
    echo "$n"; : $((n+=1))
  done
}
case "$#" in
  0) exit 1;;
  1) s "1" "$1";;
  2) s "$1" "$2";;
  3) i="$2"; s "$1" "$3";;
esac
