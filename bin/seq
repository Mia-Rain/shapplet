#!/bin/sh
seq() {
  n=$(($1)); i=${i:-1}; while [ "$n" -le $(($2-1)) ]; do
    echo "$n"; : $((n+=1))
  done
}
case "$#" in
  0) exit 1;;
  1) seq "1" "$1";;
  2) seq "$1" "$2";;
  3) i="$2"; seq "$1" "$3";;
esac
